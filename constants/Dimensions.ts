import { Dimensions } from "react-native";

export const LIST_MARGIN = 10;
export const WIDTH = Dimensions.get("screen").width - LIST_MARGIN * 2;

export const JOURNAL_ENTRY_WIDTH = Dimensions.get("screen").width * 0.9;
export const JOURNAL_ENTRY_HEIGHT = Dimensions.get("screen").height * 0.6;
